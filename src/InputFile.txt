define start()
constants
pi = 3.14
variables
firstNumber : integer ; s,t,v,n : real
begin
second firstNumber,  pi, s,t,v, var n
write n, s, pi, firstNumber
end

define second(firstNumber : integer; pi,s,t,v, var n : real)
begin
    read var n, var s, var firstNumber
end