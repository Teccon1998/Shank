define start()
constants
pi = 3.14
variables
firstNumber : integer ; s,t,v,n : real
begin
    second firstNumber,  pi, var s,t,v, var n
    write n, s, pi, firstNumber
    write firstNumber
end

define second(var firstNumber : integer; pi, var s,t,v, var n : real)
variables
i:integer
constants
begin
    if(1 > 0)
    begin
        t := 999
        if(0 <> 1)
        begin
            t := 123
        end
    end
    
    if(0 > 2)
    begin
    end
    elsif(2 > 0)
    begin
        t := 456
    end
    
    for(i from 1 to 10)
    begin
        write i
    end

    read var n, var s, firstNumber
end
